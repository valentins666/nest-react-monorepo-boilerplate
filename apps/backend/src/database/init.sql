-- Create the uuid-ossp extension for UUID generation
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Note: Tables and initial data will be handled by Knex migrations
-- This script only sets up database-level configurations and extensions
